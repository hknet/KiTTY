/* =============================================================================
 * NOT BUILT - dead code, kept for reference only.
 * Linker stubs (get_param / debug_logevent) for an old standalone keygen link,
 * superseded by the puttygen / kittygen_cli targets. Not in any CMakeLists and
 * not #included anywhere.
 * ============================================================================= */
int get_param( const char * val ) { return 0 ; }
void debug_logevent( const char *fmt, ... ) { return ; }
